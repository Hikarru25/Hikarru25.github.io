// LoginPage.jsx — Phase 7 will build this out
// Secret route (/login) — NOT in public nav
// Supabase signInWithPassword
export default function LoginPage() {
  return <div>Login Page — coming soon</div>
}
