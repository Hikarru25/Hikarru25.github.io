// BackOfficePage.jsx — Phase 8 will build this out
// Protected route (/backoffice) — auth required
// View/delete messages from Supabase, logout
export default function BackOfficePage() {
  return <div>Back Office — coming soon</div>
}
