// Header.jsx — Phase 2 will build this out fully
// Sticky header with nav links (Home, Portfolio, Links, Contact)
// Desktop: horizontal nav | Mobile: icons at bottom of screen
export default function Header() {
  return <header>Header — coming soon</header>
}
